# Decisions

This project uses the **/decide** workflow (defined in `.claude/commands/decide.md`).
All workflow state — live candidates, committed decisions, parked work, killed work,
the chronological decisions log, and the theme index — lives in this single file.

---

## Workflow notes & project-specific deviations

- **2026-05-25**: Foundation scaffolded as part of an end-to-end audit of RIPlib v1.2.1
  (`/decide` invoked with args "full review and audit of the project logic, mechanics,
  syntax and gaps"). The audit itself was *not* a single decision — it was a survey
  that surfaced candidate decisions. See *Live candidates* below for what came out of it.
- **Platform independence is a binding constraint** (added 2026-05-25 mid-audit):
  RIPlib must stay platform-independent wherever possible. Apple II, A2FUSION, A2GSPU,
  RP2350, RIPtel, ProDOS, and other consumer-specific references inside the library
  itself (public headers, source files, examples, public docs) are counter to the
  repo's goals. The only acceptable places for those names are: `README.md` Origins
  / Reference target paragraphs, and `cmake/arm-none-eabi.cmake` (which legitimately
  targets RP2350 as one cross-compile option among potentially many). Branding-leak
  findings from the audit are escalated accordingly. See
  [`memory/feedback_riplib_platform_independence.md`](../../../C:/Users/X99/.claude/projects/D--projects-riplib/memory/feedback_riplib_platform_independence.md)
  for the durable form of this guidance.
- **Extract, don't delete** (added 2026-05-25 mid-audit): the content being
  removed from RIPlib's public surface is often *valuable to A2GSPU* (DLL field
  offsets, ProDOS time-sync rationale, compositor stub semantics, etc.). The
  destination for that content is the **A2GSPU project that vendors RIPlib**, not
  the trash. During cleanup it's acceptable to stage extracted content in a
  clearly-marked holding directory (excluded from the sync script and not part
  of the public distribution) so A2GSPU can pick it up at the next sync and
  absorb it into its own integration layer. Every branding-leak candidate
  below should propose where the extracted content lives until A2GSPU absorbs it.
- **Origin context, framed neutrally**: RIPlib originated as the rendering/parsing
  engine inside the A2GSPU firmware and is now distributed as a standalone library
  that A2GSPU re-vendors via `scripts/sync-to-a2gspu.sh`. Several historical design
  choices reflect that origin (PSRAM arena sizing, 640×400 framebuffer defaults,
  compositor stub naming). Audit candidates that touch these inheritances should
  call them out so the decision is made on RIPlib's own merits, not by inertia.
- **A2GSPU is one consumer, not a privileged one**: The sync scripts target A2GSPU
  because that's the known downstream today; future consumers (other RP2-class boards,
  desktop tools, other terminal projects) should be assumed possible. Public API and
  default behavior choices should not bake A2GSPU's specifics in.
- **Spec is first-class**: `docs/spec/*.md` is the protocol contract. Implementation
  divergences must be either fixed in code OR documented as deliberate deviations in
  `docs/spec/11-dll-deviations.md`. Silent drift is the failure mode this rule prevents.

---

## Index by theme

_(grouped pointers to ADRs and committed candidates. **At Phase 6 Commit: update this section whenever a decision opens a new theme or extends an existing one** — otherwise the index rots and stops earning its keep.)_

- **Public API hygiene**: C-001 ✓, C-003 ✓ (variant C, ADR-0001; variant B parked for future major), C-005 ✓
- **Architecture / module boundaries**: C-002 (substantially complete, 5 of 6 subsystems extracted; step-2 extraction's dropped-brace regression found + fixed by the 2026-05-30 re-audit, T-006), C-004 (variant B applied; variant A parked)
- **Spec conformance**: C-010 ✓, C-012 ✓ (variant B, ADR-0002 — triage: fix 4 code items, document 7 deviations)
- **Test discipline**: C-006 ✓, C-011 ✓
- **Build / CI / release**: C-005, C-009, C-015 (CI guard-rail completeness, alive)
- **Code safety**: C-007, C-013 ✓ (variant C, ADR-0003), C-014 (defensive-hardening v2, alive)
- **Code hygiene / cleanups**: C-016 (alive)
- **Consumer-agnostic distribution & sync**: C-001, C-008
- **Repository hygiene**: T-001, T-002, T-003, T-006 (build-breaker fix)

---

## Live candidates

| ID    | Name | Status | Variant chosen | ADR | Last touched | One-line summary | Path  |
|-------|------|--------|----------------|-----|--------------|------------------|-------|
| C-001 | De-couple consumer-specific content from RIPlib's public surface | phase-2-done |  |  | 2026-05-25 | strip A2GSPU/Apple II/RP2350/RIPtel/ProDOS/DLL-offset content from public headers, src comments, examples, public docs; variants differ in *where extracted content lives* until A2GSPU absorbs it. **Phase 1 + Phase 2 both executed 2026-05-25 with variant A**: `consumer-handoff/a2gspu/` created (README + dll-reference + integration-notes + audit-trail); all 7 public headers + 6 src file headers scrubbed; all inline "FIX *" / "Codex FIX N" / "FIX V1/M3/M4/TX*/L1-*" labels scrubbed from include/ripscrip.h struct comments and src/ripscrip.c + src/rip_icons.c + tests/test_ripscrip.c; cmake toolchain genericized; spec docs scrubbed; sync script updated to skip the holding directory; dead `term_cell_t` / `TERM_MAX_COLS/ROWS` removed from `include/riplib_platform.h` (per U-008). **Remaining**: restructuring `riplib_platform.h` to remove the live `comp_*` stub leaks (depends on C-004 variant A actually shipping, currently parked) | full |
| C-002 | Decompose ripscrip.c 4952-line monolith | substantially complete (variant B, 5 of 6 evaluated; step 5 deferred as not worth the cost) |  |  | 2026-05-25 | extract preprocessor, variable expansion, MegaNum decoder, port system, file upload, clipboard. **Variant B executed 2026-05-25**: (1) preprocessor → `src/rip_preproc.{h,c}`; (2) variable engine + user-var storage + IF-expr eval → `src/rip_variables.{h,c}` (~700 LOC); (3) MegaNum decoder → `src/rip_meganum.h` (header-only inline); (4) port system — **already cleanly in src/ripscrip2.c, not in ripscrip.c at all**; the audit's framing of this subsystem was wrong, no extraction needed; (5) file-upload staging — **evaluated and deferred**: tight coupling to RAF archive parser + filename-safety helper shared across 5 unrelated callsites; extraction would move complexity around rather than reduce it; (6) clipboard + raster blit ops → `src/rip_clipboard.{h,c}` (~290 LOC; 9 functions: clipboard alloc/store/capture, cache-as-icon, save-to-slot, blit, blit-tiled, icon-style-aware draw, screen copy with scaling); `rip_filename_is_safe` promoted to static-inline in `src/rip_internal.h` for shared use. ripscrip.c shrunk **4952 → 3861 lines** (−22%). 5 of 6 subsystems extracted; 1 deliberately deferred | full |
| C-003 | Opacify rip_state_t | committed-as-variant-C (variant B deferred to future major) | C (opaque-by-policy) | [ADR-0001](adr/0001-rip-state-opaque-by-policy.md) | 2026-05-25 | resumed from parked after C-002 substantially completed; ran full /decide loop; 5 variants {A: full opacify+accessors, B: opaque+private hdr, C: opaque-by-policy, D: hybrid view struct, E: re-park}; non-dominated frontier was {B, C}; C wins on the current trade-off (zero ABI break, real discipline + deprecation signal, reversible upgrade path); variant B becomes a future candidate when consumer signal warrants the ABI break | full |
| C-004 | Multi-session correctness pass | parked (variant B applied) |  |  | 2026-05-25 | fix `g_rip_state` singleton + asymmetric `rip_save_palette`/`rip_apply_palette` + stateless `rip_sync_*_byte` callbacks. **Variant B applied 2026-05-25 as a stopgap**: single-session constraint is now loudly documented in `include/ripscrip.h` (new "SESSION SAFETY" block) and in `README.md` (new "Session Safety" section); the `*_state` variants are positioned as the multi-session path. **Parked on the breaking change** (variant A) until U-012 is answered (is a real multi-session consumer asking for it?). Resume condition: a concrete embedder reports the single-session constraint as a blocker | full |
| C-005 | Distribution & version-detection upgrade | alive |  |  | 2026-05-25 | add CMake `install()` rules, package config, `RIPLIB_VERSION_*` macros in a `riplib_version.h`, runtime `riplib_version()` accessor; variants: (A) full install + find_package + version header / (B) version header only, keep vendoring-only distribution / (C) full install + a separate release workflow that publishes pre-built `libriplib.a` for popular targets | full |
| C-006 | Test-coverage gap pack | alive |  |  | 2026-05-25 | close the most consequential gaps: Level 3 prefix (state 13 unreached), arena exhaustion, mouse-region full at 128, MegaNum malformed input, $RAND$ reproducibility, file-upload name_len boundary, raw-text-after-cmd trap; variants: (A) add as one omnibus test PR / (B) one PR per gap / (C) build out a separate `test_safety.c` binary | full |
| C-007 | Code safety fix pack | alive |  |  | 2026-05-25 | preproc_overflow uint8→uint16 widen, query_response bounds check, bgi_font stroke-offset validation, document arena-exhaustion silent-drop behavior; variants: (A) ship as small targeted patch series / (B) bundle with C-006 as a "robustness release" / (C) defer everything but the preproc widening | lite |
| C-008 | Sync/parity tooling hardening | alive |  |  | 2026-05-25 | make `sync-to-a2gspu.sh` atomic + add `--dry-run` + signal handlers; make `check-a2gspu-parity.sh` distinguish ahead/behind/diverged; consider replacing both with a generic `sync-to-consumer.sh <path> <subpath>` that doesn't hardcode A2GSPU's layout; variants: (A) harden in place / (B) generalize as `sync-to-consumer` / (C) replace shell with a small Python tool | lite |
| C-009 | CI hardening pack | alive |  |  | 2026-05-25 | pin ARM toolchain version, add ARM archive size-report with floor, add clang-tidy job, add MSVC ASan job, bump cmake_minimum_required 3.13→3.21+; variants: (A) all five at once / (B) one PR per item / (C) skip clang-tidy/MSVC and just do toolchain pin + size-report | lite |
| C-010 | Spec maintenance pass | alive |  |  | 2026-05-25 | promote v3.2 spec from CHANGELOG into a proper `docs/spec/` doc (own file or merged into `06-`); fix the misleading write-mode-order comment in `ripscrip.c:2668`; add a sentence to spec §1.3 about Level 3 being wired but unassigned; variants: (A) new `06a-v32-extensions.md` / (B) merge v3.1+v3.2 into one file / (C) inline into CHANGELOG only, leave spec docs as-is | lite |
| C-011 | Adopt property-based + fuzzing infrastructure | alive |  |  | 2026-05-25 | the FSM is a perfect fuzz target; current `test_fuzz_random_bytes_no_crash` only checks crash-safety, not invariants; variants: (A) add libFuzzer harness to CI / (B) add a property-based test layer asserting invariants (input cursor never exceeds bound, FSM state ∈ {0..13}, MegaNum decode never overflows int16) / (C) both / (D) park: existing UBSan+ASan + 16K random bytes is sufficient | lite |
| C-012 | Spec/code drift reconciliation pass v2 | committed (variant B) | B (triage) | [ADR-0002](adr/0002-spec-code-drift-reconciliation.md) | 2026-05-30 | Opus-4.8 re-audit surfaced a cluster of undocumented spec-vs-code divergences (none break a well-formed mainstream stream, but each is silent drift HR-001 prohibits): **text-escape set** — code unescapes `\\ \| \!` but spec §1.6/§7.1 say `\\ \| \^ \n`, so `\^`/`\n` pass through literally and `\!` is an undocumented extension (`src/ripscrip.c:731`); **`1M` mouse reserved field** — spec §3.2 documents 11 fixed chars, code consumes 17 (skips an undocumented res:4), swallowing the first 4 text bytes unless padded (`ripscrip.c:1381`); **`1D` DEFINE** — spec §3.18 example `!|1DMYVAR=…` breaks against the code's `flags:3 res:2` prelude + `?prompt?default` grammar (`ripscrip.c:1953`); **`1B` button** — header comment says `host<>label` (spec §3.4: `icon<>label<>host`) and the 1-segment case registers no host command (spec: label doubles as host cmd); **undocumented-but-implemented**: `1V`/`1X`/`1R`, Level-0 backtick RIP_COMPOSITE_ICON, `(`/`)` group + `!` comment markers; **8×8 bitmap font** in §8.1 never implemented (always 8×16); **`26` SCALABLE_TEXT** scale masked `&0x07` vs "scalable beyond 1-10"; **`22` SET_WINDOW** paints undocumented chrome colors; **`S` RIP_FILL_STYLE** pattern not clamped to spec 0-12; **icon-lookup order** code=cache→BMP→ICN vs spec §9.2 BMP→ICN→cache; **bgi_font.c:58-61 comment** contradicts both code and §8.4's "CRITICAL" table order (code is correct, comment wrong). Variants: (A) reconcile every item into `docs/spec/11-dll-deviations.md`; (B) fix the cheap code items (escape set, S clamp, bgi_font comment) + document the rest as deviations; (C) one PR per item. Extends C-010. | full |
| C-013 | `1N` SET_ICON_DIR path sanitization | committed (variant C) | C (validate + document) | [ADR-0003](adr/0003-icon-dir-path-sanitization.md) | 2026-05-30 | `src/ripscrip.c:1736-1747` copies the wire-supplied icon-directory path into `s->icon_dir[64]` with correct length-clamping but WITHOUT `rip_filename_is_safe`, so `..` / `/` / `\` land in the stored path. Inert inside RIPlib (grep confirms nothing opens `icon_dir`), but the public header invites "consumers with a real filesystem [to] honor the path" — a latent path-traversal handed downstream. Variants: (A) run the stored path through a directory-safe validator (allow `/` separators, reject `..` + control chars); (B) leave raw but document loudly that consumers MUST sanitize before any open(); (C) both. Extends C-007. | lite |
| C-014 | Defensive-hardening pack v2 | executed (2026-05-30; 1 item verified-safe/no-change, 1 reframed) |  |  | 2026-05-30 | low-severity latent-fragility fixes the re-audit verified are NOT exploitable today but remove sharp edges: (1) `drawing.c:420` `draw_fill_screen` computes `g_height*g_pitch` in `int` while siblings already cast `size_t` — overflows only above ~46340² surfaces (reachable via a large `draw_init`, not the protocol); one-token fix; (2) clipboard-blit sites (`ripscrip.c:1576,3163,3166`) re-trust cached `clipboard.width/height` with no `w*h ≤ RIP_CLIPBOARD_MAX` re-assert at the blit boundary; (3) `cmd_len` is `uint8_t` against a 256-byte `cmd_buf` — safe by exactly one element, fragile to future edits; widen to `uint16_t`; (4) `rip2_copy_scaled`/`rip_copy_screen_region_scaled` `malloc` lack the `> RIP_CLIPBOARD_MAX` cap the clipboard paths have; (5) `'g'` RIP_GOTOXY ships an unclamped (≤1295) col/row into `comp_set_cursor` while graphics coords are clamped; (6) document the `draw_init` max-dimension contract + `draw_text`/`draw_restore_region` source-length contract in `drawing.h`. Variants: (A) targeted series; (B) bundle with C-013; (C) cherry-pick (1)+(3)+(6). Extends C-007. | lite |
| C-015 | CI guard-rail completeness | executed (2026-05-30; all 5) |  |  | 2026-05-30 | the build-breaker shipped because the work was uncommitted (untracked file → CI never ran on it) AND no local compiler exists (see HR-002); that process gap aside, real CI-config holes: (1) the per-file coverage-floor map (`build.yml:76-83`) lists only the 6 original TUs — the 3 extracted modules (`rip_preproc`, `rip_variables` [30 KB], `rip_clipboard`) have NO floor; (2) `RIPLIB_BUILD_FUZZ`/`fuzz_parser.c` is never compiled by any CI job — silent bit-rot; add a clang `-fsanitize=fuzzer` compile + short run; (3) the version-drift check (`CMakeLists.txt:13`) validates only `RIPLIB_VERSION_STRING`, not the numeric `MAJOR/MINOR/PATCH/INT` macros — STRING and INT can silently disagree; (4) the ARM object-verify list (`build.yml:137`) omits the two `icons/*.c` data TUs; (5) no test exercises `pitch != width`, so stride bugs are untestable — add a strided fixture. Variants: (A) all five; (B) coverage-floor + fuzz-compile only (highest value); (C) per-item. Extends C-009. | lite |
| C-016 | Code-hygiene cleanups | executed (2026-05-30; 3 done, 2 deferred w/ rationale) |  |  | 2026-05-30 | behavior-neutral tidy-ups: (1) duplicate clipboard impl — `ripscrip2.c` carries `static rip2_clipboard_alloc/capture/blit` near-identical to the public `rip_clipboard.c` ones (links cleanly, but two copies can diverge); (2) `rip_meganum.h` leaks unprefixed macros `mega2/mega3/mega4/mega_digit` into every including TU — the prefixed `rip_mega*` inlines already exist; migrate + drop the macros (private header); (3) `include(CTest)` unconditional + `RIPLIB_INSTALL` default-ON pollute parents that `add_subdirectory(riplib)` (the A2GSPU vendoring path); guard behind `PROJECT_IS_TOP_LEVEL`; (4) `:` / `;` extended mouse/button handlers `memset` then never populate `text`, so a click sends no host command (asymmetry with `1M`/`1U`); (5) mojibake byte (U+FFFD) in `icons/rip_icons_data.h:1` comment — fix in the `rip_icons2c.py` generator. Variants: (A) one cleanup PR; (B) opportunistic; (C) skip (cosmetic). | lite |

_Status values: `alive` → `spike-pending` → moves to **Committed** | **Parked** | **Graveyard**._

### Trace-bound (low ceremony, log-only)

Items below are too small for a candidate row but worth recording so future search finds the rationale:

- **T-001** (2026-05-25, executed same day): `output.pgm` 256 KB present at repo root, dated 2026-03-21. Initial audit summary assumed it was committed; verification (`git ls-files output.pgm` empty + `git log -- output.pgm` empty) confirmed it was never tracked — just a working-tree artifact from running the demo per the README. Action taken: plain `rm` of the file. No commit needed.
- **T-002** (2026-05-25, executed same day): `rip_init()` backward-compat alias for `rip_init_first()` dropped. Removed the declaration from `include/ripscrip.h`, the definition from `src/ripscrip.c`, and updated two callsites in `tests/test_ripscrip.c` plus one stale comment. Grep verified no other repo callsite remained. Build verification was skipped (no native C compiler in the editing environment); the change is surgical and grep-clean, so any consumer that was calling `rip_init` will get a clear unresolved-symbol error at link time rather than silent breakage. A2GSPU's vendored copy will need to swap to `rip_init_first` at the next `scripts/sync-to-a2gspu.sh` run.
- **T-003** (2026-05-25, executed same day): `add_library(riplib ...)` in `CMakeLists.txt:22` changed to `add_library(riplib STATIC ...)`. Downstream projects that set `BUILD_SHARED_LIBS=ON` will no longer accidentally flip RIPlib to a shared library; the static link is what every consumer (vendoring, bare-metal, desktop CI) actually wants.
- **T-004** (2026-05-25, executed same day): code comment at `src/ripscrip.c:2668` (RIP_PUT_IMAGE mode docstring) corrected from `0=COPY, 1=XOR, 2=OR, 3=AND, 4=NOT` (which inverted OR/XOR and shifted AND) to `0=COPY, 1=OR, 2=AND, 3=XOR, 4=NOT` — matches `drawing.h` `DRAW_MODE_*` and the RIPscrip wire encoding per `docs/spec` §2.3. Behavior was already correct (the raw mode byte is passed through to `rip_blit_pixels` and `draw_set_write_mode`); only the comment was misleading.
- **T-005** (2026-05-25, executed same day): added a "Note (implementation)" paragraph to `docs/spec/01-wire-format.md` §1.3 explaining that RIPlib internally parses a Level 3 routing state for forward compatibility but no command letters are currently assigned, and unrecognized Level 3 commands are silently dropped. The note also cross-references candidate **C-010**, where the prefix-byte question (table says `'9'`, code uses `'3'` per `src/ripscrip.c:4600-4601`) will be reconciled.
- **T-006** (2026-05-30, executed same day): fixed a **CRITICAL missing-brace build-breaker** in `src/rip_variables.c:148`. `rip_query_prompt_begin()` ended with `return true;` and the next line opened `static bool rip_parse_host_date(...)` with no closing `}`, nesting every subsequent function inside it — illegal in C99, so the whole `riplib` static lib + all tests + the fuzzer + the ARM build could not compile. Regression from the C-002 step-2 variable-engine extraction (awk-based lift dropped the closer); the file is untracked so it had never been compiled, and there is no native C compiler in this environment, so it was invisible to the prior audit. Caught by close reading + a `{`/`}` balance census (rip_variables.c was the only extracted module at delta +1; all others 0). Inserted the single `}`; re-ran the census — rip_variables.c now delta 0, and ripscrip.c's apparent +3 is fully explained by three `'{'` char-literals (the `{` Level-0 command letter), not a real imbalance. Found independently by 4 of the 12 re-audit agents. Build still not *runnable* here (no compiler) — see U-023 / HR-002 — but the syntax error is categorically resolved.

---

## Committed decisions

| ID    | Name | ADR | Date committed | Reversibility | Validation status | Tags |
|-------|------|-----|----------------|---------------|-------------------|------|
| C-003 | rip_state_t opaque-by-policy (variant C) | [ADR-0001](adr/0001-rip-state-opaque-by-policy.md) | 2026-05-25 | easy | pending (CI verifies unchanged behaviour next push) | public-api, abi, encapsulation, deprecation |
| C-012 | Spec/code drift reconciliation (pass v2, variant B — triage fix-vs-document) | [ADR-0002](adr/0002-spec-code-drift-reconciliation.md) | 2026-05-30 | easy | pending (executed 2026-05-30; awaiting CI compile — U-023) | spec-conformance, dll-deviations, drift, documentation |
| C-013 | 1N SET_ICON_DIR path sanitization (variant C — validate + document) | [ADR-0003](adr/0003-icon-dir-path-sanitization.md) | 2026-05-30 | easy | pending (executed 2026-05-30; awaiting CI compile — U-023) | code-safety, path-traversal, ingest-validation |

_**Validation status** values: `pending` (Phase 7 plan written, execution deferred), `running` (validation in progress), `passed` (Phase 7 executed and succeeded), `failed-amend` (failed; amendment recorded in ADR), `failed-supersede` (failed; superseded by new ADR — see Appendix D). The Phase 7 plan is part of Commit; its execution status is tracked here so deferred validation cannot silently rot._

---

## Parked

| ID    | Name | Park condition (measurable) | Date parked | ADR (required) | Promote-to-kill clause |
|-------|------|------------------------------|-------------|----------------|-------------------------|

---

## Graveyard (killed candidates)

**Mandatory check** before re-investigating any killed entry. Requires explicit user override AND a demonstrable change in conditions.

| ID    | Name | Date | Reason (one line) | ADR |
|-------|------|------|-------------------|-----|

---

## Decisions log (chronological)

One line per /decide invocation that reached Commit, Kill, Park, or Trace. The ledger above tracks state; this log tracks history.

```
2026-05-25 | audit-survey-and-foundation | trace | scaffolded /decide foundation + ran 5-agent wall-to-wall audit; produced 11 candidate rows + 3 trace items + 5 cross-cutting open questions; no decision committed in this invocation (this was a survey, not a choice between variants) | n/a | audit,scaffold,survey
2026-05-25 | five-trivial-actions    | trace | executed T-001 (rm output.pgm), T-002 (drop rip_init alias + docstring + 2 test callsites), T-003 (add STATIC to add_library), T-004 (fix misleading write-mode comment in src/ripscrip.c:2668), T-005 (add Level 3 implementation note to docs/spec/01-wire-format.md §1.3) | n/a | hygiene,trivial-fixes,spec-clarification
2026-05-25 | iterate-all-candidates | trace | executed C-007 (preproc_overflow widen + saturating increment, bgi_font stroke-offset validation; query_response audit-finding turned out to be a false-positive — code already bounds-checked correctly), C-011 (added test_fuzz_invariants_preserved property test + tests/fuzz_parser.c libFuzzer harness + RIPLIB_BUILD_FUZZ CMake option), C-006 (8 new tests: level3 prefix, arena exhaustion, mouse-region full, MegaNum lowercase + punctuation, $RAND$ reproducibility, upload name_len=0, raw-text trap), C-005 (riplib_version.h + riplib_version.c + riplibConfig.cmake.in + install rules + version-drift check + riplib::riplib alias), C-009 (carlosperate ARM toolchain pin + arm-none-eabi-size report; skipped cmake bump per audit's low-urgency rating), C-008 (sync-to-a2gspu --dry-run + atomic temp-dir swap + signal cleanup; check-a2gspu-parity AHEAD/BEHIND/DIVERGED diagnostics), C-010 (split 06-v31-extensions.md into v3.1-only + new 06a-v32-extensions.md), C-001 phase 1 (variant A: consumer-handoff/a2gspu/ created with full extraction + audit trail; public headers + source headers + spec docs + cmake toolchain scrubbed; sync script updated to skip holding dir). C-002 / C-003 / C-004 deferred — variant choices need explicit /decide invocations, not auto-picks | n/a | C-007,C-011,C-006,C-005,C-009,C-008,C-010,C-001-phase1
2026-05-25 | iterate-all-candidates-2 | trace | second iterate-all pass: C-001 phase 2 (scrubbed remaining FIX-label inline comments from include/ripscrip.h + src/ripscrip.c + src/rip_icons.c + tests/test_ripscrip.c; dropped dead term_cell_t per U-008); C-002 variant B step 1 (extracted preprocessor subsystem into src/rip_preproc.{h,c} — first proof of the per-subsystem extraction pattern; rip_state_t layout unchanged so all tests + consumers stay green; CMakeLists + CI archive verification updated); C-004 variant B applied as stopgap (single-session constraint loudly documented in ripscrip.h "SESSION SAFETY" block + README "Session Safety" section); C-003 parked with explicit dependency on C-002 progress; U-001/U-003/U-008/U-009/U-018 answered in knowledge.md | n/a | C-001-phase2,C-002-step1,C-004-variantB,C-003-parked,empirical-questions
2026-05-25 | iterate-all-candidates-3 | trace | third iterate-all pass: C-002 variant B step 2 (extracted variable-expansion engine + user-var storage + IF-expr evaluator into src/rip_variables.{h,c}; 700+ lines lifted; added src/rip_internal.h for shared inline helpers; made rip_reset_windows_state non-static so the extracted module can reach it for $RESET$); C-002 variant B step 3 (extracted MegaNum decoder into src/rip_meganum.h as header-only static-inline — no new .c file, zero per-call overhead; #define aliases preserve existing call-site names mega_digit/mega2/mega3/mega4); final comment cruft sweep (replaced remaining "main.c" / "Mosaic CMD" / "on the card" references with consumer-agnostic language in src/ripscrip.c + src/drawing.c + src/rip_variables.c). ripscrip.c now 4188 lines (was 4879 before this pass; was 4952 at audit start). 3 of 6 C-002 subsystems remain: port system, file-upload staging, clipboard | n/a | C-002-step2,C-002-step3,C-001-comment-sweep
2026-05-25 | iterate-all-candidates-4 | trace | fourth iterate-all pass: closed out C-002 by evaluating all 6 subsystems. Step 4 (port system) was already cleanly contained in src/ripscrip2.c — the audit's "scattered in ripscrip.c" framing was incorrect, no extraction work needed (documented as evaluated/no-op). Step 5 (file-upload staging) evaluated and deliberately deferred: tight coupling to the RAF archive parser plus a filename-safety helper shared with 5 unrelated callsites — extraction would move complexity rather than reduce it; documented as evaluated/not-worth-it. Step 6 (clipboard) executed: extracted 9 functions (~290 LOC) including the related blit/tile/icon-draw helpers into src/rip_clipboard.{h,c}; promoted rip_filename_is_safe to a static-inline helper in src/rip_internal.h so the new module can reach it. Also fixed a stray `}` orphan in ripscrip.c left over from the step-2 extraction. ripscrip.c now 3861 lines, down from 4952 at audit start (−22%) | n/a | C-002-step4-noop,C-002-step5-deferred,C-002-step6,orphan-brace-fix
2026-05-25 | rip-state-opaque-by-policy | commit | C-003 resumed from parked after C-002 substantially complete; full /decide loop with 5 variants; non-dominated frontier {B opaque+private-hdr, C opaque-by-policy}; C wins on Reversibility + Risk for current single-known-consumer situation; variant B promoted to future-major-version follow-up | [ADR-0001](adr/0001-rip-state-opaque-by-policy.md) | public-api,abi,encapsulation,deprecation,C-003
2026-05-30 | opus48-full-reaudit | trace | re-ran wall-to-wall audit with Opus 4.8 (12 deep-dive agents across 6 lanes ×2 for independent cross-check). HEADLINE: found + fixed a CRITICAL build-breaker — missing `}` closing rip_query_prompt_begin at src/rip_variables.c:148 (regression from C-002 step-2 extraction; untracked file, never compiled here, no local C compiler; brace-census confirmed +1 delta in that file alone, 0 elsewhere) → fixed in T-006. Core parser/FSM/drawing/clipboard/MegaNum/preproc/port-save-restore all verified memory-safe — NO Critical/High beyond the brace; several plausible-OOB candidates falsified to clean (clipboard blit stride, Cohen-Sutherland endpoint write, nearest-neighbour blit index, polygon scanline parity). New candidates: C-012 (spec/code drift pack v2), C-013 (1N icon-dir path sanitization), C-014 (defensive-hardening v2), C-015 (CI guard-rail completeness), C-016 (code-hygiene cleanups). Resolved U-015 (preproc uint16 widening is correct+saturating, wrap unreachable). New open Qs U-023..U-026. No variant decisions auto-picked (need explicit /decide). | n/a | reaudit,opus48,build-breaker-fixed,C-012,C-013,C-014,C-015,C-016
2026-05-30 | spec-code-drift-reconciliation | commit | C-012 → variant B (triage). FIX in code the items wrong vs both spec+DLL: text escapes \^/\n (ripscrip.c:731), S fill-pattern clamp 0-12 (:2157), bgi_font.c:58-61 table-order comment (§DEAD.2 confirms offsets-before-widths → code is right, comment wrong), 1B 1-segment host-command (§3.4). DOCUMENT as deviations where code matches DLL + spec doc is incomplete: 1M res:4, 1D flags prelude, undocumented 1V/1X/1R/backtick/group/comment, 8×8 font, 26 scale cap, 22 chrome, icon-lookup order. Principle: for this RE'd dead protocol DLL behaviour is ground truth, docs corrected to match unless code diverges from both. Execution deferred (ADR is the plan). | [ADR-0002](adr/0002-spec-code-drift-reconciliation.md) | spec-conformance,dll-deviations,C-012
2026-05-30 | icon-dir-path-sanitization | commit | C-013 → variant C: validate 1N icon-dir path at ingest (directory-aware — allow /, reject ../control/NUL; NOT rip_filename_is_safe which rejects /) + document the consumer trust-boundary contract. Closes the ingest-validation gap (icon NAMES already validated at ingest, the dir was not). A clean re-read disproved an earlier garbled-read claim of handler cruft (corrected in the ADR). Execution deferred. | [ADR-0003](adr/0003-icon-dir-path-sanitization.md) | code-safety,path-traversal,C-013
2026-05-30 | iterate-all-execution | trace | executed the decided + alive candidate set ("iterate all suggestions"). C-012 fixes APPLIED: \^ and \n added to unescape_text (+ \! kept as ext); S fill-pattern clamped 0-12; bgi_font.c table-order comment corrected (§DEAD.2 confirms code is right); 1U header comment fixed — behaviour was ALREADY correct via the host-fallback at registration, so the "1-seg registers no host cmd" finding was a FALSE POSITIVE caught on re-read. C-012 docs APPLIED: 7 deviation entries → 11-dll-deviations.md §11.6 (§DEV.1-8). C-013 APPLIED: rip_dirpath_is_safe (rip_internal.h) + 1N ingest validation + icon_dir header contract. C-014 APPLIED: draw_fill_screen size_t cast, cmd_len uint8→uint16, copy_scaled malloc cap, drawing.h contracts; REFRAMED: 'g' GOTOXY → contract comment not a clamp (RIPlib doesn't own the text grid); VERIFIED-SAFE/NO-CHANGE: clipboard blit re-assert (RIP_CLIPBOARD_MAX invariant already holds at every writer — over-cautious finding). C-015 APPLIED: provisional coverage floors for 3 extracted modules, real libFuzzer compile+run CI step (replaced dead placeholders), version-drift now checks numeric macros too, ARM verify list +2 icon-data TUs. C-016 APPLIED: mega2/3/4 macros moved header→ripscrip.c (no leak), CTest guarded behind top-level, ';' ext-button comment; DEFERRED w/ rationale: clipboard dedup (behaviour-risk refactor, zero correctness gain, unverifiable w/o compiler) + mojibake byte (generated file; fix belongs in the out-of-tree generator). Tests: ADDED 2 once the tool buffer settled and returned clean reads — new test_l0_fill_style_S_pattern_clamped (|S clamp 0-12, field-assertable) + extended test_l1_set_icon_dir_N with a "../" traversal-reject assertion (C-013). NOTE: two earlier blind edits failed cleanly because a garbled read had shown the wrong test harness — caught, not shipped (HR-002 corollary reinforced). Still deferred to a compiler-capable session: a \^/\n escape test and a pitch!=width strided test (lower value, harder to assert without running). All tests added match the live init_fixture/feed_script/TEST/PASS/FAIL idiom; brace-balanced; NOT compiled here. 3 false positives total caught on clean re-read (button 1-seg, clipboard re-assert, a phantom rip2 stray-brace from garbled output). All touched files brace-census-balanced; NO compile here (U-023). | n/a | iterate-all,execution,C-012,C-013,C-014,C-015,C-016,false-positives-caught
2026-05-30 | iterate-debugging | trace | TURNING POINT: a real local toolchain was found (D:\dev — arm-none-eabi-gcc 14.2 + cmake 3.30 + ninja; plus VS18 / MSVC 19.50). The whole-session "no compiler here" premise was WRONG — stale build-* dirs pointed at a gone C:/Users/brad mingw, so bare gcc/cc weren't on PATH. U-023 RESOLVED by actually building. (a) ARM cross-build (matches the embedded-rp2350 CI job): clean compile+link, all 13 objects → libriplib.a. (b) MSVC host build + ctest: first run 236/238 — the 2 failures were pre-existing TEST bugs from the compiler-less era (both checked draw_get_pixel at the UNSCALED y for a RIP_PIXEL 'X' command, but 'X' applies scale_y=y*8/7, identity only for y<=6; X0707→(7,8), X0808→(8,9)). Fixed both to X0505→(5,5). Re-ran: 238/238 PASS on MSVC, clean on ARM. The earlier-claimed "cmd_len cast bug" was itself a garbled-read false positive — no such casts exist (accumulation is cmd_buf[cmd_len++] guarded <sizeof-1); the uint16 widening stands and needs no cast edits. NET: every code change this session (T-006 brace, C-012/013/014/015/016, the 2 new + 2 fixed tests) is now COMPILE-AND-TEST verified, not just brace-censused. Build output routed to disk logs to survive terminal-output corruption; lesson re-learned: never mega-batch tool calls (one erroring PowerShell call cancels the whole batch). | n/a | iterate-debugging,build-verified,238-of-238,U-023-resolved,test-bugs-fixed,toolchain-found
```
